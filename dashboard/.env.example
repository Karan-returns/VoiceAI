MONGODB_URI=mongodb://127.0.0.1:27017/novatel
DASHBOARD_PORT=3456

# Team access key — teammates enter this on the login screen (unset = auth disabled for local dev)
# Generate: openssl rand -hex 24
# DASHBOARD_API_KEY=

# Restrict CORS to your hosted URL in production
# DASHBOARD_CORS_ORIGIN=https://your-app.onrender.com
